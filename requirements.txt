#all of these dependencies for Python 3.12.0
spacy==3.8.2                # For API development
numpy~=1.26.4
scikit-learn==1.5.2         # For sentiment analysis
textblob==0.18.0            # For TF-IDF and other ML methods
gensim==4.3.2               # train and generate Word2Vec embeddings
thinc==8.3.2
scipy==1.11.4
transformers==4.45.2
#python -m spacy download en_core_web_sm
#python -m textblob.download_corpora
#install PyTorch
    #For CPU-only (no GPU support): pip install torch
    #For GPU Support (CUDA): pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
fastapi==0.115.2
Pygments==2.18.0
pydantic==2.9.2